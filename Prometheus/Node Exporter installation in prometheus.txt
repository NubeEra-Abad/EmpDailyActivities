# go promethues.io

# go download section 

# copy node exproter link 

$ wget https://github.com/prometheus/node_exporter/releases/download/v1.5.0/node_exporter-1.5.0.linux-amd64.tar.gz

$ tav -xvf node_exporter-1.1.0.linux-amd64

$ sudo cp node_exporter-1.1.0.linux-amd64/node_exproter /usr/local/bin

$ sudo nano /etc/systemd/system/node-exporter.service


	[Unit]
	Description=Prometheus Node Exporter Service
	After=network.target

	[Service]
	Type=simple
	ExecStart=/usr/local/bin/node_exporter

	[Install]
	WantedBy=multi.user.target


$ sudo systemctl daemon-reload

$ sudo service node-exporter start

$ sudo service node-exporter service

check 192.168.0.1:9100